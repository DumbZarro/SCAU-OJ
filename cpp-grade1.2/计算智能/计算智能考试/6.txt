（1）
由程序看最复杂的地方是一个二重循环，把一次判断两串的元素是否相等的过程if(s[i-1]==ss[j-1])当作单位时间
若两个字符串的长度分别为m和n，则该程序的时间复杂度为O（mn）
（2）
法一：若改成最长公共子串则题目几乎就变为“匹配字符串”的变式，设计一个匹配字符串函数，并选择一个较短的子串，
从长到短的划分出子串并且把它当作模版串，循环与里一个串匹配。一个n长的串大约有n^2个子串。
法二：直接在原程序上改，把数组改为一个pair数组，并把判断语句的结果改一下，正确的continue，else 记录（i，j-1），
循环结束之后再计算比较谁的j-1最大，然后输出第一个串的a[i][j-1]。